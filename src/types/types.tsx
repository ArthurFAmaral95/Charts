export type SideBarOpen = {
  sideBarOpen: boolean
}

export type Count = {
  count: number
}

export type HandleSideBar = {
  handleSideBar: () => void
}

export type ChangePage = {
  changePage: (page: string) => void
}
